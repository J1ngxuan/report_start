# 研究内容与方法边界简述

## 1. 机器人构型与运动学基础

本研究以轮式双臂机器人为实验平台。机器人上半身由双七自由度机械臂、四自由度腰部和二自由度头部组成，并配置末端夹爪及多视角视觉传感器。现阶段研究主要面向双臂操作任务，以左右机械臂末端笛卡尔位姿和夹爪状态作为策略学习的核心状态与动作变量；腰部、头部和移动底盘作为系统扩展自由度，为后续全身协同操作研究提供基础。

机器人运动学模型采用改进 Denavit–Hartenberg（Modified Denavit–Hartenberg，MDH）参数描述机械臂连杆关系。正运动学用于建立关节空间到末端笛卡尔空间的映射，逆运动学则根据目标末端位姿、当前关节状态及关节限位求取可行关节构型。在此基础上，笛卡尔控制模块将规划或策略生成的末端目标位姿转换为关节指令，并结合轨迹平滑、关节约束、冗余自由度调节和奇异位形处理，实现机械臂末端运动控制。上述运动学与控制功能属于机器人平台已有能力，本文主要将其作为学习策略与物理机器人之间的执行接口。

## 2. VR 遥操作与示范数据采集

本研究采用虚拟现实设备构建机器人遥操作示范采集系统。系统获取头显和双手控制器的空间位姿及交互信号，通过坐标系变换、参考位姿标定和运动增量映射，将操作者的手部运动转换为机器人左右末端的笛卡尔运动指令，同时利用手柄输入控制夹爪开合。该方式能够以较低操作门槛采集包含双臂协调行为的连续操作示范。

在数据记录过程中，系统同步采集机器人末端状态、遥操作动作以及头部和腕部相机图像，并依据动作时刻完成多模态数据对齐，形成按任务回合组织的示范轨迹。由此获得的数据包含多视角视觉观测、机器人本体状态和连续动作序列，可用于训练视觉条件下的机器人操作策略。当前示范数据以双臂末端位姿和夹爪动作为主要有效信息，腰部、头部、底盘及力触觉信息是否纳入后续研究，需根据最终任务方案进一步确定。

## 3. 模型训练与闭环执行

模型训练以遥操作示范轨迹为数据基础，学习从多视角视觉观测和机器人当前状态到未来连续动作序列的映射。现有训练框架支持基于动作分块的模仿学习方法，通过视觉特征提取、状态表征与时序动作建模，预测一定时间范围内的双臂笛卡尔动作。相较于逐时刻预测，动作分块能够增强操作序列的时序一致性，并降低频繁推理对实时性的影响。

在在线执行阶段，机器人首先获取当前视觉观测和末端状态，随后由策略模型生成动作序列，并通过笛卡尔控制接口驱动机械臂执行。完成设定长度的动作后，系统重新获取环境观测并进行下一轮推理，从而形成“状态感知—策略推理—动作执行—状态更新”的闭环控制过程。现有代码已经具备示范采集、离线训练、模型部署和闭环推理的基本条件，但训练后的实机数据尚未自动用于策略更新，因此目前属于离线训练与在线闭环执行相结合的技术路线，而非自动迭代学习闭环。
