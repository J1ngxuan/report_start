% !Mode:: "TeX:UTF-8"
% 默认配置：哈尔滨校区硕士开题报告。
% 可选学位类型：type=doctor|master|bachelor
% 可选校区：campus=harbin|shenzhen|weihai
\documentclass[toc=true,type=master,stage=opening,campus=harbin]{hithesisart}

\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,calc}
\graphicspath{{figures/}}

% 兼容 footmisc 6：切换为模板的页底对齐时，清除上一页缓存的页底胶。
% 保留 \flushbottom 的默认排版行为，避免旧缓存触发分页拆箱错误。
\makeatletter
\ifdefined\@outputbox@reinsertbskip
  \AddToHook{cmd/flushbottom/after}{%
    \global\let\@outputbox@reinsertbskip\relax
  }
\fi
\makeatother

% 四级标题（“项”）按学校规范排版：题序前空4个半角字符，
% 采用全角括号编号，标题后的阐述内容空4个半角字符接排。
\ctexset{%
  secnumdepth=paragraph,
  paragraph={
    number={（\arabic{paragraph}）},
    indent={2\ccwd},
    beforeskip={3bp},
    afterskip={2\ccwd},
    aftername={},
    format={\heiti\normalsize},
    afterindent=true,
    fixskip=true,
    runin=true,
  },
}
\makeatletter
\@addtoreset{paragraph}{subsection}
\makeatother

% 用灰色文字标出待填写位置，完成撰写后可将本命令改为直接输出正文。
\newcommand{\placeholder}[1]{%
  \par\noindent\textcolor{gray}{【填写提示：#1】}\par
}

\begin{document}
\input{front/cover}
\hypersetup{pageanchor=false}
\makecover
\hypersetup{pageanchor=true}
\pagenumbering{arabic}
\input{body/proposal}
\end{document}

% Local Variables:
% TeX-engine: xetex
% End:
