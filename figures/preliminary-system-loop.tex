\begin{tikzpicture}[
  font=\small,
  >={Stealth[length=2.2mm]},
  source/.style={draw=violet!65!black,rounded corners=2pt,fill=violet!7,
    minimum height=14mm,text width=27mm,align=center,inner sep=2.5pt},
  process/.style={draw=blue!65!black,rounded corners=2pt,fill=blue!7,
    minimum height=14mm,text width=27mm,align=center,inner sep=2.5pt},
  robot/.style={draw=orange!75!black,rounded corners=2pt,fill=orange!10,
    minimum height=14mm,text width=28mm,align=center,inner sep=2.5pt},
  data/.style={draw=teal!65!black,rounded corners=2pt,fill=teal!7,
    minimum height=14mm,text width=27mm,align=center,inner sep=2.5pt},
  arrow/.style={->,thick,blue!70!black},
  dataarrow/.style={->,thick,teal!70!black},
  trainarrow/.style={->,thick,densely dashed,violet!70!black},
  feedback/.style={->,thick,orange!80!black,rounded corners=3pt}
]
  \node[source] (vr) {\textbf{VR遥操作}\\双手位姿\\与夹爪输入};
  \node[process,right=5mm of vr] (mapping) {\textbf{坐标映射}\\参考位姿标定\\与运动增量};
  \node[process,right=5mm of mapping] (controller) {\textbf{笛卡尔接口}\\逆运动学\\与轨迹约束};
  \node[robot,right=5mm of controller] (robot) {\textbf{轮式双臂机器人}\\导航停靠后\\双臂执行与多视角感知};

  \draw[arrow] (vr) -- (mapping);
  \draw[arrow] (mapping) -- (controller);
  \draw[arrow] (controller) -- (robot);

  \node[data,below=13mm of robot] (record) {\textbf{同步记录}\\图像·末端状态\\动作·时间戳};
  \node[data,left=7mm of record] (dataset) {\textbf{示范数据集}\\按任务回合组织\\语言指令关联};
  \node[source,left=7mm of dataset] (pi) {\textbf{OpenPI \(\pi_{0.5}\)微调}\\流匹配动作专家\\主线模型};
  \node[source,below=4mm of pi] (act) {\textbf{ACT微调}\\CVAE+动作分块\\对照模型};
  \node[process,left=7mm of pi] (online) {\textbf{真机部署}\\在线推理\\输出动作块};

  \draw[dataarrow] (robot) -- (record);
  \draw[dataarrow] (record) -- (dataset);
  \draw[trainarrow] (dataset) -- (pi);
  \draw[trainarrow] (dataset.south) |- (act.east);
  \draw[arrow] (pi) -- (online);
  \draw[arrow] (act.west) -| (online.south);
  \draw[arrow] (online.north) -- ++(0,6mm) -| (controller.south);
  \draw[feedback] (robot.east) -- ++(5mm,0) |- ([yshift=-6mm]act.south)
    -| node[pos=0.25,below,font=\footnotesize]{部署时新观测反馈} ([xshift=-4mm]online.west) -- (online.west);
\end{tikzpicture}
