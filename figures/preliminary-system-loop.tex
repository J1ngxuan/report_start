\begin{tikzpicture}[
  font=\small,
  >={Stealth[length=2.2mm]},
  box/.style={rounded corners=2pt,minimum width=29mm,minimum height=13mm,
    text width=27.2mm,align=center,inner sep=2.5pt,outer sep=0pt},
  process/.style={box,draw=blue!60!black,fill=blue!7},
  physical/.style={box,draw=orange!75!black,fill=orange!10},
  train/.style={box,draw=violet!65!black,fill=violet!7},
  mainarrow/.style={->,thick,blue!70!black},
  trainarrow/.style={->,thick,densely dashed,violet!70!black},
  feedback/.style={->,thick,orange!80!black,rounded corners=3pt},
  linklabel/.style={font=\footnotesize,inner sep=1pt,text=blue!65!black},
  onlinelabel/.style={linklabel,fill=white},
  trainlabel/.style={linklabel,text=violet!70!black},
  vallabel/.style={linklabel,text=orange!80!black}
]
  \node[physical] (vr) {\textbf{VR遥操作}\\双手位姿输入};
  \node[process,right=8mm of vr] (mapping) {\textbf{坐标映射}\\位姿增量映射};
  \node[process,right=8mm of mapping] (controller) {\textbf{笛卡尔接口}\\逆运动学求解};
  \node[physical,right=8mm of controller] (robot) {\textbf{轮式双臂机器人}\\双臂执行与感知};
  \draw[mainarrow] (vr) -- node[linklabel,above]{位姿} (mapping);
  \draw[mainarrow] (mapping) -- node[linklabel,above]{增量} (controller);
  \draw[mainarrow] (controller) -- node[linklabel,above]{指令} (robot);

  \node[process,below=12mm of robot] (record) {\textbf{同步记录}\\图像·状态·动作};
  \node[process,below=12mm of controller] (dataset) {\textbf{示范数据集}\\回合与语言指令};
  \node[train,below=12mm of mapping] (pi) {\textbf{OpenPI微调}\\\(\pi_{0.5}\)动作头适配};
  \node[process,below=12mm of vr] (online) {\textbf{OpenPI推理}\\输出连续动作序列};
  \node[process,below=8mm of pi] (state) {\textbf{任务状态机}\\低层语言指令};
  \draw[mainarrow] (robot) -- node[onlinelabel,right=1mm]{同步} (record);
  \draw[mainarrow] (record) -- node[linklabel,above]{回合} (dataset);
  \draw[trainarrow] (dataset) -- node[trainlabel,above]{训练} (pi);
  \draw[trainarrow] (pi) -- node[trainlabel,above]{权重} (online);
  \draw[mainarrow] (state.west) -| node[pos=0.25,onlinelabel,above]{指令} (online.south);
  \draw[mainarrow] (online.north) -- ([yshift=6mm]online.north) -|
    node[pos=0.25,onlinelabel,above]{动作序列} (controller.south);
  \draw[feedback] (robot.east) -- ([xshift=4mm]robot.east) |-
    ([yshift=-5mm]state.south) -|
    node[pos=0.25,vallabel,below]{部署时新观测反馈} ([xshift=-4mm]online.west) --
    (online.west);
\end{tikzpicture}
