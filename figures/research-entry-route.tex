\begin{tikzpicture}[
  font=\small,
  >={Stealth[length=2.2mm]},
  stage/.style={draw=blue!60!black,rounded corners=2pt,fill=blue!7,
    minimum height=29mm,text width=27mm,align=center,
    inner xsep=2.5pt,inner ysep=2.5pt,outer sep=0pt},
  validate/.style={stage,draw=orange!75!black,fill=orange!10},
  issue/.style={draw=red!55!black,rounded corners=4pt,fill=red!4,
    minimum height=13mm,text width=27mm,align=center,
    inner xsep=2.5pt,inner ysep=2pt,outer sep=0pt,font=\footnotesize},
  metrics/.style={issue},
  arrow/.style={->,thick,blue!70!black,shorten >=1pt,shorten <=1pt},
  feedback/.style={->,thick,densely dashed,orange!80!black,
    rounded corners=3pt,shorten >=1pt,shorten <=1pt},
  issuearrow/.style={->,densely dashed,red!65!black,
    shorten >=1pt,shorten <=1pt}
]
  % 两行节点使用相同宽高与统一列间距，保证边框严格对齐。
  \node[stage] (pipeline) {\textbf{已贯通\(\pi_{0.5}\)接口}\\VR遥操作数采\\$\downarrow$\\OpenPI微调与单回合执行};
  \node[stage,right=4mm of pipeline] (observe) {\textbf{真机失败观察}\\停靠偏差下\\精细操作阶段\\出现失败现象};
  \node[stage,right=4mm of observe] (chain) {\textbf{残差传递链}\\位置等幅传递\\航向随距离转化\\块内持续至反馈};
  \node[validate,right=4mm of chain] (defense) {\textbf{分层鲁棒增强框架}\\条件化学习\\自适应控制\\运行时容错};

  \draw[arrow] (pipeline.east) -- (observe.west);
  \draw[arrow] (observe.east) -- (chain.west);
  \draw[arrow] (chain.east) -- (defense.west);

  \node[issue,below=8mm of pipeline] (dataissue) {示范仅在名义\\停靠位姿下采集};
  \node[issue,below=8mm of observe] (failissue) {残差与失败概率\\尚待分档量化};
  \node[issue,below=8mm of chain] (chainissue) {状态无显式残差\\块内不接收新观测};
  \node[metrics,below=8mm of defense] (metric) {容忍边界\(R_{80}\)\\检测·恢复·时延};

  \draw[issuearrow] (dataissue.north) -- (pipeline.south);
  \draw[issuearrow] (failissue.north) -- (observe.south);
  \draw[issuearrow] (chainissue.north) -- (chain.south);
  \draw[issuearrow] (metric.north) -- (defense.south);

  % 反馈标线分段指定锚点，保持全程水平/垂直，并用白底保护标注文字。
  \coordinate (feedback-east) at ([yshift=-7mm]metric.south);
  \coordinate (feedback-west) at ([yshift=-7mm]dataissue.south);
  \draw[feedback]
    (metric.south) -- (feedback-east) --
    node[midway,above=1pt,fill=white,inner xsep=2pt,inner ysep=0.5pt,
      font=\footnotesize,text=orange!80!black]
      {验证结果反馈数采规范与部署配置}
    (feedback-west) -- (dataissue.south);
\end{tikzpicture}
