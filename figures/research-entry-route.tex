\begin{tikzpicture}[
  font=\small,
  >={Stealth[length=2.2mm]},
  stage/.style={draw=blue!60!black,rounded corners=2pt,fill=blue!7,
    minimum height=21mm,text width=24mm,align=center,inner sep=2.5pt},
  validate/.style={draw=orange!75!black,rounded corners=2pt,fill=orange!10,
    minimum height=21mm,text width=27mm,align=center,inner sep=2.5pt},
  issue/.style={draw=red!55!black,rounded corners=4pt,fill=red!4,
    text width=24mm,align=center,inner sep=2pt,font=\footnotesize},
  metrics/.style={draw=red!55!black,rounded corners=4pt,fill=red!4,
    text width=27mm,align=center,inner sep=2pt,font=\footnotesize},
  arrow/.style={->,thick,blue!70!black},
  feedback/.style={->,thick,densely dashed,orange!80!black,rounded corners=3pt},
  issuearrow/.style={->,densely dashed,red!65!black}
]
  \node[stage] (task) {\textbf{任务与评价协议}\\观测·动作\\约束·扰动};
  \node[stage,right=5mm of task] (prior) {\textbf{示教先验学习}\\数据采集与对齐\\$\downarrow$\\模仿策略};
  \node[stage,right=5mm of prior] (ppo) {\textbf{PPO策略优化}\\标准基线\\与候选改进};
  \node[validate,right=5mm of ppo] (validation) {\textbf{分级验证}\\仿真对比\\$\downarrow$\\安全准入→真机};

  \draw[arrow] (task) -- (prior);
  \draw[arrow] (prior) -- (ppo);
  \draw[arrow] (ppo) -- (validation);

  \node[issue,below=8mm of prior] (dataissue) {数据覆盖与示教噪声};
  \node[issue,below=8mm of ppo] (learnissue) {样本效率·先验遗忘\\稀疏奖励·动作平滑};
  \node[metrics,below=8mm of validation] (metric) {成功·节拍·接触力\\成本·扰动退化·安全};

  \draw[issuearrow] (dataissue) -- (prior);
  \draw[issuearrow] (learnissue) -- (ppo);
  \draw[issuearrow] (metric) -- (validation);
  \draw[feedback] (metric.south) -- ++(0,-5mm) -|
    node[pos=0.55,below,font=\footnotesize]{多维评价反馈} (task.south);
\end{tikzpicture}
