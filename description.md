
# 面向 PCBA上下料的轮式双臂模仿学习策略鲁棒性改进研究

## 进度

目前对双臂使用ACT和OpenPI打通了VR遥操作数采———模型微调————真机部署链条

## 场景

涉及多工位，通过传统算法+激光雷达导航实现工位精准移动

1. pcba上料百叶车夹取
2. pcba扫码
3. 从检测台取出上一块pcba
4. 将pcba放入检测台
5. 将上一块pcba放入下料百叶车

## 配置

1. 当前有轮式双臂4台，1台可供采集，4台可供实验
2. 有八卡A100 80G 考虑一张卡可供使用

## 主要研究内容

### 研究主线

轮式双臂在多工位间移动并作业，策略失败的根源可以归结为一条误差传递链：底盘停车带来厘米级的初始误差；这一误差在进槽、落台等精细操作阶段被放大为夹取或放置偏差；偏差超过策略的纠正能力后，策略不会自行停止，而是继续执行错误动作，最终导致失败甚至损坏工件。

本研究沿这条误差链依次设防，形成三个层层递进的改进点：

1. **事前，误差感知。** 让策略在进入操作阶段之前就感知并补偿停车误差（改进点一），从源头减少进入精细操作阶段的误差。
2. **事中，在线纠偏。** 让策略在精细操作阶段以更高的频率闭环修正（改进点二），处理改进点一无法完全消除的残余误差，把策略的容忍边界向外推。
3. **事后，失败兜底。** 对前两层仍无法处理的失败进行在线检测和安全恢复（改进点三），作为最后一道防线。

三层之间不只是逻辑上的依次兜底，在技术和实验上也彼此依赖：改进点一的容忍曲线是后两层共同的评价基准；改进点二在精细阶段更频繁地重新推理，为改进点三提供了更密集的重叠预测作为失败信号；改进点一的残差注入手段则为改进点三提供了可控的失败样本。三者的实验按"基线、加一、加一二、加一二三"的顺序逐层叠加，最终给出完整系统的端到端验证。

### 改进点一：误差感知，将停车残差引入策略状态输入

**问题背景。** 轮式底盘依靠激光雷达导航停靠到工位后，仍不可避免地存在厘米级的位置与朝向偏差。若策略在训练时只见过"标准停车位"下的数据，部署时的停车误差会直接转化为末端执行器的操作偏差，导致夹取或放置失败。这是整条误差链的起点，也是最先需要处理的环节。

**方法。** 从数据和输入两个层面同时处理这一问题：

1. 数据采集阶段，在每个工位 ±3 cm 范围内随机停车，使训练数据本身覆盖不同的停车偏差；
2. 将激光雷达定位给出的机器人相对工位的偏差 (dx, dy, dθ) 拼接进策略的状态向量，使策略能够显式感知当前停车误差，并据此对整体动作进行补偿。

**实验设计。**

- 容忍曲线：部署时人为注入 0 到 5 cm 的停车残差，绘制任务成功率随残差大小变化的曲线，量化策略对停车误差的容忍范围。
- 消融对照：设置三组实验。(a) 基线，固定位置停车且不输入残差；(b) 随机停车扩充数据覆盖，但不输入残差；(c) 随机停车并输入残差。比较 (b) 与 (a) 的差异可得到"数据覆盖"的贡献，比较 (c) 与 (b) 的差异可得到"残差输入"的贡献。

**在研究主线中的作用。** 本改进得到的容忍曲线是后续两项改进的共同基准：它给出了策略仅依靠状态输入做"粗补偿"时的容忍边界。改进点二的目标是把这条边界向外推，改进点三的目标是在边界之外把失败安全地兜住。

**依据。** 该改进点是 Mobile ALOHA 论文笔记中所建议方案的直接实现。

### 改进点二：在线纠偏，分阶段的动作块执行策略

**问题背景。** 改进点一让策略在进入操作阶段时"知道"自己偏了多少，并据此调整整体动作。但这种补偿来自单次状态输入，是粗粒度的；对于进槽、落台等毫米级配合的接触阶段，残余误差需要依靠视觉反馈在执行过程中持续修正。ACT 一类策略每次推理预测一个动作块（action chunk），随后连续执行其中的动作。固定的块长在两类场景之间存在矛盾：自由空间中的大范围运动希望块长一些，以减少推理次数、降低时延；而精细接触阶段则希望块短一些，以便及时根据最新观测修正动作。

**方法。** 该改进仅修改推理侧的执行逻辑，不需要重新训练模型，因此可以直接叠加在改进点一训练好的策略之上。按任务阶段动态调整每次推理后实际执行的步数：在自由空间移动阶段，执行完整的长动作块；进入进槽、落台等精细操作阶段后，每次只执行动作块的前几步便重新推理，从而提高闭环控制频率。

**实验设计。** 在改进点一的策略基础上进行。以固定块长 20、50、100 三档作为对照组，与分阶段执行方案比较，评价指标同时报告任务成功率和任务时延，以验证该方法能够在不牺牲整体效率的前提下提高精细操作阶段的成功率。同时在注入残差的条件下重复改进点一的容忍曲线实验，检验闭环频率的提高是否使容忍边界外移。

**在研究主线中的作用。** 改进点二在改进点一"粗补偿"的基础上加上了"细修正"，二者分别处理误差链的源头和放大环节。此外，精细阶段更频繁地重新推理，使时序集成在这些阶段拥有更多相互重叠的预测，而这正是改进点三所依赖的信号来源：失败最容易发生的阶段，恰好也是失败信号最密集的阶段。

**依据。** ACT 与 OpenVLA-OFT 两篇论文笔记均直接给出了这一建议。

### 改进点三：失败兜底，基于预测不一致度的失败预警与恢复

**问题背景。** 经过前两层改进，策略的容忍范围得到扩展，但仍会遇到超出容忍边界的情况，例如停车误差过大或 PCBA 在百叶车中的位置异常。此时模仿学习策略通常不会自行停止，而是继续执行错误动作，可能造成 PCBA 损坏或机械臂卡死。因此需要一种低成本、可在线运行的失败检测与恢复机制，作为整个系统的最后一道防线。

**方法。** ACT 的时序集成（temporal ensemble）机制本身会对同一时刻产生多个相互重叠的动作预测。本研究将这些重叠预测之间的方差作为"策略不一致度"信号：预测一致说明策略对当前状态有把握，方差骤增则提示策略进入了不熟悉的状态，失败风险升高。具体流程如下：

1. 阈值标定：在改进点一、二叠加后的配置下，统计成功回合中不一致度的分布，取其 95 分位数作为预警阈值；
2. 在线监测：推理过程中实时计算不一致度，超过阈值即判定为失败风险；
3. 安全恢复：触发后机械臂退回预设的安全高度，然后重试一次。恢复动作采用固定模板执行，不由模型生成，以保证恢复过程本身可靠、可控。

**实验设计。** 利用改进点一的残差注入手段，在容忍边界之外制造受控的失败样本，评估检测器的召回率、误报率和预警提前量；在此基础上报告加入恢复机制后的最终任务成功率，以及恢复带来的额外时延。

**在研究主线中的作用。** 改进点三不试图进一步扩大容忍范围，而是接管前两层处理不了的部分。它不需要额外训练，信号来自改进点二增强后的重叠预测，评估样本来自改进点一的残差注入，因此是建立在前两层之上的自然延伸。

**定位。** 该方案可视为 FAIL-Detect 方法的免训练简化版本：去掉了其中的密度估计模块，直接复用策略已有的多重预测作为失败信号，无需额外训练检测器。

### 整体验证

三项改进按上述顺序逐层叠加，形成四个配置：基线、加改进点一、加改进点一二、加改进点一二三。四个配置在同一套残差注入条件和同一套五工位任务上比较端到端成功率与任务时延。这样既能看出每一层的边际贡献，也能给出完整系统的最终性能，并与研究主线中"事前、事中、事后"三道防线一一对应。
